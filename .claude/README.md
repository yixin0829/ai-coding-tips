# Claude Code Configuration

This folder contains my Claude Code configuration files and related scripts.

Hard linked files:
- `.claude/statusline.ps1` -> `%USERPROFILE%\.claude\statusline.ps1`
- `.claude/settings.json` -> `%USERPROFILE%\.claude\settings.json`

Change in one file will be reflected in the other.